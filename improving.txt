Se você tiver **vários produtos** e cada um tem um atributo `data-preco` diferente, você pode seguir um fluxo semelhante, mas ajustando o código para **gerenciar múltiplos valores de preços**. Vamos imaginar que você tenha vários produtos em sua página, cada um com um valor de `data-preco`. Quando o usuário clica em um botão, o valor do produto será subtraído do saldo do usuário.

Aqui está o que você pode fazer:

### Passos principais:

1. **Recuperar todos os produtos** (e seus preços) da página.
2. **Subtrair o preço do produto selecionado** do saldo do usuário.
3. **Atualizar o `localStorage`** com o novo saldo após a compra.

### Código de exemplo para múltiplos produtos:

```html
<div id="produtos">
  <div class="produto" data-preco="100">
    <p>Produto 1</p>
    <button class="btnComprar">Comprar</button>
  </div>
  <div class="produto" data-preco="150">
    <p>Produto 2</p>
    <button class="btnComprar">Comprar</button>
  </div>
  <div class="produto" data-preco="200">
    <p>Produto 3</p>
    <button class="btnComprar">Comprar</button>
  </div>
</div>

<script>
  // Recupera os registros do localStorage, ou um array vazio se não existirem registros
  let Registros = JSON.parse(localStorage.getItem("Registros")) || [];
  let ultimoRegistro = Registros.length > 0 ? Registros[Registros.length - 1] : null;

  if (ultimoRegistro) {
    alert(`Olá ${ultimoRegistro.nome}, o seu saldo é de ${ultimoRegistro.valor}`);
  } else {
    alert("Não há registros.");
  }

  // Seleciona todos os botões de compra
  document.querySelectorAll('.btnComprar').forEach(button => {
    button.addEventListener('click', function() {
      let produto = this.closest('.produto'); // Encontra o produto relacionado ao botão clicado
      let productValue = parseFloat(produto.getAttribute('data-preco')); // Recupera o valor do produto

      if (ultimoRegistro) {
        let valorUser = parseFloat(ultimoRegistro.valor); // Garante que valorUser seja um número

        if (!isNaN(valorUser) && !isNaN(productValue)) { // Verifica se ambos são números válidos
          let total = valorUser - productValue;

          if (total >= 0) {
            alert(`Resta agora ${total} na sua conta.`);
            ultimoRegistro.valor = total; // Atualiza o saldo

            // Atualiza o array de registros com o novo saldo
            Registros[Registros.length - 1] = ultimoRegistro;
            localStorage.setItem("Registros", JSON.stringify(Registros)); // Atualiza no localStorage
          } else {
            alert("Saldo insuficiente.");
          }
        } else {
          alert("Valores inválidos.");
        }
      } else {
        alert("Usuário não encontrado.");
      }
    });
  });
</script>
```

### Explicação do código:

1. **Estrutura HTML**:
   - Temos várias `div` com a classe `.produto`, cada uma contendo um atributo `data-preco` que armazena o preço do produto.
   - Cada produto tem um botão com a classe `.btnComprar`.

2. **Recuperando e verificando o usuário**:
   - A primeira parte do código recupera o último registro de saldo do `localStorage` e exibe o saldo atual do usuário.
   
3. **Gerenciamento de múltiplos produtos**:
   - Para cada botão de compra (com a classe `.btnComprar`), o evento `click` é escutado.
   - Quando o botão é clicado, ele encontra o `data-preco` do produto associado e o valor do saldo do usuário (`valorUser`).
   
4. **Verificação e atualização do saldo**:
   - Verifica se o valor do saldo e o preço do produto são números válidos com `isNaN()`.
   - Se o saldo for suficiente para a compra, o saldo é subtraído pelo preço do produto e o `localStorage` é atualizado.
   - Se o saldo for insuficiente, um alerta é mostrado ao usuário.
   
5. **Atualizando o `localStorage`**:
   - Após a compra, o saldo do usuário é atualizado e o array `Registros` é salvo novamente no `localStorage`.

### O que este código faz:

- **Gerencia múltiplos produtos**: Ele permite que o usuário compre qualquer produto na lista, subtraindo o valor do produto selecionado do seu saldo.
- **Atualiza o `localStorage`** com o novo saldo a cada compra.
- **Exibe mensagens de erro** se o saldo for insuficiente ou se os valores não forem válidos.

### Possíveis melhorias:

- **Exibir o preço antes de comprar**: Você pode adicionar um `alert` ou um visual na página para exibir o preço do produto selecionado antes de confirmar a compra.
- **Limitar a quantidade de compra**: Se quiser adicionar quantidade de produtos (por exemplo, o usuário pode comprar vários itens), você pode alterar o código para verificar a quantidade e fazer a subtração com base nela.

Se você precisar de mais detalhes ou ajustes, fique à vontade para perguntar! 😊